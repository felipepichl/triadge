const HEADER_HEIGHT = 60
const MAX_BLUR_INTENSITY = 30

const Colors = {
  background: '#121214', // $gray700
  headerBackground: '#202024', // $gray600
  white: '#E1E1E6', // $gray100
}

const spacing = {
  xs: 4,
  sm: 15,
  md: 16,
  lg: 24,
  xl: 32,
  '2xl': 48,
}

export { HEADER_HEIGHT, MAX_BLUR_INTENSITY, Colors, spacing }
